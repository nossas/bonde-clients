import styled from 'styled-components'

/**
 * The `ListItem` children wrapper.
 */
const List = styled.ul`
  list-style: none;
  padding: 0;
  margin: 0;
  overflow-x: hidden;
  text-overflow: ellipsis;
`

/* @component */
export default List
