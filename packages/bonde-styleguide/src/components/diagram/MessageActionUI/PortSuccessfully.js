import styled from 'styled-components'
import { PortDynamicStyle } from '../BaseUI/Port'

const PortSuccessfully = styled.div`
  .port {
    ${PortDynamicStyle}
    background-color: green;
  }
`

export default PortSuccessfully