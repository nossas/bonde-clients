import React, { PropTypes } from 'react'


const Menu = props => {
  return (
    <div></div>
  )
}

Menu.propTypes = {
  blocks: PropTypes.array.isRequired,
}

Menu.defaultProps = {
  blocks: [],
}

export default Menu
