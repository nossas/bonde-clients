export const isLoaded = state => state.blocks.loaded

export const isLoading = state => state.blocks.loading

export const getList = state => state.blocks.data

export const isEditionMode = state => state.blocks.editionMode
