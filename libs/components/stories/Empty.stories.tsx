import React from 'react';
import { action } from '@storybook/addon-actions';
import { Empty } from '@';

export const empty = () =>
  <Empty message='Nada por aqui...' />
;

export default {
  title: 'Empty',
};