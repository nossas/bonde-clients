machine:
  timezone:
    America/Sao_Paulo # Set the timezone
  node:
    version: 6.2.1

test:
  pre:
    - bundle exec rake db:migrate

deployment:
  production:
    branch: master
    commands:
      - ./bin/deploy.sh
  feature:
    branch: /add-.*/
    commands:
      - ./bin/deploy.sh
